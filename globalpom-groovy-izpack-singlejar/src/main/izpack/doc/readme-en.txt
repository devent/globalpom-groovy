Start

To start the application there are scripts installed in the bin/ directory.
For Linux it is the bin/linux/run.sh script, for Windows it is the 
bin/windows/run.bat script. The script must be run in the directory of the 
script, i.e.: 

    $ cd bin/linux
    $ ./run

